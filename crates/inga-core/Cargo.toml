[package]
name = "inga-core"
description = "Lexer, parser, type/effect inference, and formatter for the Inga language"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
