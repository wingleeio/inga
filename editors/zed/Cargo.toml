[package]
name = "zed-inga"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"

# Standalone crate: not part of the Inga cargo workspace.
[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.2.0"
